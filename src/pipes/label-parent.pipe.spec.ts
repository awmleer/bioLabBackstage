import { LabelParentPipe } from './label-parent.pipe';

describe('LabelParentPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelParentPipe();
    expect(pipe).toBeTruthy();
  });
});
