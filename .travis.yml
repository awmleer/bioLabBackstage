language: node_js
node_js:
  - "node"
before_install:
  - sudo apt-get update
  - sudo apt-get install -y sshpass
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s
  - export PATH="$HOME/.yarn/bin:$PATH"
install:
  - yarn install
script:
  - yarn run build
cache:
  yarn: true
  directories:
    - "node_modules"
addons:
  ssh_known_hosts: 49.234.197.80
deploy:
  provider: script
  skip_cleanup: true
  script:  sshpass -p "$SERVER_PWD" rsync -avz $TRAVIS_BUILD_DIR/dist/* root@49.234.197.80:/var/www/bioLab/backstage/
  on:
    branch: master
notifications:
  email:
    on_success: never
    on_failure: always
